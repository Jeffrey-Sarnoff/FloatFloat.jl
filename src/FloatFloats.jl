module FloatFloats

import Base: hash, string, 
    show, showcompact, showall,
    convert, promote_rule,
    isequal, isless, 
    (==),(!=),(<),(<=),(>=),(>),
    (+),(-),(*),(/),(\),(%),(^)

export FloatFloat, FF

end # module
